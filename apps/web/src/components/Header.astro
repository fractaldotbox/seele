---
---
    <header class="bg-white shadow">
      <div class="container mx-auto px-6 py-4 flex justify-between items-center">
        <h1 class="text-2xl font-bold text-primary">Bloomberg</h1>
        <nav>
          <ul class="flex space-x-6">
            {["Markets", "Economy", "Politics", "Tech"].map((item) => (
              <li key={item}>
                <a href={`#${item.toLowerCase()}`} class="text-gray-700 hover:text-primary">
                  {item}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </div>
    </header>
